# Local demo (CPU or CUDA torch): greedy vs sampling vs beam
transformers>=4.45
# install torch from https://download.pytorch.org/whl/cpu for a small CPU-only
# build, or the default index for CUDA. torch>=2.2 works.
torch>=2.2

# §4 benchmark only: JSON-schema constrained decoding via HF
# prefix_allowed_tokens_fn (the vLLM guided_json equivalent). Needs the
# transformers-v5 shim in src/experiment.py:make_prefix_fn.
lm-format-enforcer>=0.10

# Production path (needs an NVIDIA GPU with compute capability >= 7.0):
# vllm>=0.9
# openai>=1.40   # only for --server mode
